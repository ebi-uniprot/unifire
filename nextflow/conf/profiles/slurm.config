params {
    iprscan6ProfileName = "slurm"
}

process {
    withLabel:use_gpu {
        containerOptions = '--nv'
        clusterOptions = '--gres=gpu:1'
    }
}

docker {
    enabled = false
}

singularity {
    enabled = true
}
