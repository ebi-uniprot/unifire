params {
    iprscan6ProfileNames = (params.iprscan6ProfileNames ?: []) + ['singularity']
}

process {
    withLabel:use_gpu {
        containerOptions = '--nv'
    }
}

singularity {
    enabled = true
    pullTimeout = '3 hours' // the default is 20 minutes and fails with large images
}
