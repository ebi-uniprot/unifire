params {
    iprscan6ProfileName = "podman"
}

process {
    withLabel:use_gpu {
        containerOptions = '--gpus all'
    }
}

podman {
    enabled = true
    registry = 'docker.io'
    mountFlags = 'Z'
}
