params {
    iprscan6ProfileName = "apptainer"
}

process {
    withLabel:use_gpu {
        containerOptions = '--nv'
    }
}

apptainer {
    enabled = true
    pullTimeout = '3 hours' // the default is 20 minutes and fails with large images
}

singularity {
    enabled = true
}