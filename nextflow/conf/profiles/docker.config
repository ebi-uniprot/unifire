params {
    iprscan6ProfileNames = (params.iprscan6ProfileNames ?: []) + ['docker']
}

process {
    withLabel:use_gpu {
        containerOptions = '--gpus all'
    }
}

docker {
    enabled = true
    mountFlags = 'Z'
}
